---
description: Check code quality and style
agent: build
---

Run linting and code quality checks across the codebase.

1. Run the configured linter (ruff, flake8, or similar)
2. Run type checker (mypy) if available
3. List all issues found, grouped by severity
4. Suggest fixes for common issues
5. Note any style violations

Provide clear, actionable feedback on how to fix each issue.
