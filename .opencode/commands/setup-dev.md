---
description: Set up local development environment
---

Help me set up the local development environment for urban-garbanzo.

1. **Check current setup**:
   - List what's in the project root (dependencies, config files)
   - Identify the tech stack (web framework, database, testing tools)

2. **Provide exact commands to**:
   - Create and activate a Python virtual environment
   - Install all dependencies (dev + prod)
   - Set up pre-commit hooks if they exist
   - Initialize any databases or local services needed
   - Verify the setup works (e.g., run a smoke test)

3. **Create a quick reference**:
   - Show the one-liner to get started
   - List common commands (test, lint, format, dev server)
   - Note any environment variables that need to be set

Make sure commands are copy-paste ready and platform-aware if needed.
